# py_importers

Install pre-requisites:

```bash
python -m venv env
source env/bin/activate
pip install -r requirements.txt
```

Run:

```bash
python gambit.py
python pouet.py
```

Entries are built into the `py_common/beta/` folder
